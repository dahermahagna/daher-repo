# Home Assistant Podman Environment Configuration
# Copy this file to .env and update with your actual values

# ==========================================
# DIRECTORY CONFIGURATION
# ==========================================
# Base directory where all container data will be stored
BASE_DIR=/opt/homeassistant

# ==========================================
# POD CONFIGURATION
# ==========================================
# Name of the Podman pod
POD_NAME=homeassistant-pod

# ==========================================
# TIMEZONE
# ==========================================
# Set your timezone (e.g., America/New_York, Europe/London, Asia/Tokyo)
TZ=America/New_York

# ==========================================
# PORT CONFIGURATION
# ==========================================
# Home Assistant web interface port
HA_PORT=8123

# Mosquitto MQTT broker port
MOSQUITTO_PORT=1883

# Zigbee2MQTT frontend port
Z2M_FRONTEND_PORT=8080

# Configurator web interface port
CONFIGURATOR_PORT=3218

# MariaDB database port (internal to pod)
MARIADB_PORT=3306

# ==========================================
# MARIADB CONFIGURATION
# ==========================================
# MariaDB root password (use a strong password!)
MARIADB_ROOT_PASSWORD=change_this_root_password

# MariaDB database name for Home Assistant
MARIADB_DATABASE=homeassistant

# MariaDB user for Home Assistant
MARIADB_USER=ha_user

# MariaDB password for Home Assistant user (use a strong password!)
MARIADB_PASSWORD=change_this_ha_password

# ==========================================
# MQTT CONFIGURATION
# ==========================================
# MQTT broker username
MQTT_USER=mqtt_user

# MQTT broker password (use a strong password!)
MQTT_PASSWORD=change_this_mqtt_password

# ==========================================
# ZIGBEE2MQTT CONFIGURATION
# ==========================================
# Authentication token for Zigbee2MQTT frontend (use a strong token!)
Z2M_AUTH_TOKEN=change_this_z2m_token

# ==========================================
# CONFIGURATOR CONFIGURATION
# ==========================================
# Password for Configurator web interface (use a strong password!)
CONFIGURATOR_PASSWORD=change_this_configurator_password

# ==========================================
# NOTES
# ==========================================
# 1. Make sure to change ALL passwords and tokens above!
# 2. The Zigbee dongle will be auto-detected by the setup script
# 3. All containers run in the same pod and can communicate via localhost
# 4. Ports are published on the host for external access
# 5. Data persistence is handled through volume mounts to BASE_DIR
